const nome = "Batatinha";
const idade = 18;
const email = "batatinha.da.silva@gmail.com"
const aceitouTermo = false;

if (nome !== "" && idade >= 18 && email !== "" && aceitouTermo) {
    console.log("Cadastro válido")
} else {
    console.log("Cadastro inválido")
}